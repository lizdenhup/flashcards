<% if @deck.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@deck.errors.count, "error") %> prohibited this deck from being saved:</h2>
 
    <ul>
    <% @deck.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= f.hidden_field :user_id, :value => current_user.id %>
  <table>
    <tr>
    <td><%= f.label :name %></td>
    <td><%= f.text_field :name %></td>
    </tr>
  <%= f.fields_for :subject, Subject.new do |subject| %>
    <tr>
    <td><%= subject.label :name, 'Subject' %></td>
    <td><%= subject.text_field :name %></td>
    </tr>
  </table>
<% end %>